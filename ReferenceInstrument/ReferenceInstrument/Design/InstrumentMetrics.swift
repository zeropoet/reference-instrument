import SwiftUI

enum InstrumentMetrics {
    static let cellSize: CGFloat = 64
    static let gridSpacing: CGFloat = 12

    static let gridWidth: CGFloat =
        (cellSize * 4) + (gridSpacing * 3)

    static let lockSize: CGFloat = 8
}
